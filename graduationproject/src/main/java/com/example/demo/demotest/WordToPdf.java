package com.example.demo.demotest;


import org.jodconverter.JodConverter;
import org.jodconverter.office.*;

public class WordToPdf {
    /*File inputFile = new File("document.doc");
    File outputFile = new File("document.pdf");
    LocalOfficeManager.Builder officeManager = LocalOfficeManager.builder().officeHome("C:\\Program Files (x86)\\OpenOffice 4");
    final LocalOfficeManager officeManager1 = LocalOfficeManager.install();
        try {

            officeManager1.start();
            JodConverter.convert(inputFile).to(outputFile).execute;
        } catch (OfficeException e) {
            e.printStackTrace();
        }finally{
        LocalOfficeUtils.stopQuietly(officeManager1);
       }*/

}
